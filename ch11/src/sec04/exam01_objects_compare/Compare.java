package sec04.exam01_objects_compare;

public class Compare {
	/*
		객체 비교 Objects.compare(T a, T b, Comparator<T> c))
		1. a, b 두 객체를 비교자(c)로 비교하여 int값으로 리턴
		2. Comparator<T> 인터페이스
		 - 제네릭 타입으로 T타입의 객체를 비교하는 compare(T a, T b) 메소드를 가지고있다.
		 - compare(T a, T b)메소드를 재정의해서 a와 b를 비교하는 코드를 작성해야 한다. 
	 */
}
