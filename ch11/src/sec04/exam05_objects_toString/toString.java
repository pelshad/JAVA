package sec04.exam05_objects_toString;

public class toString {
	/*
		객체 문자정보(toString())
		1. 객체의 문자정보를 리턴한다.
		 toString(Object o)
		  : not null -> o.to.String()
		  : null -> "null"
		 toString(Object o, String nullDefault)
		  : not null -> o.to.String()
		  : null -> nullDefault
	 */
}
